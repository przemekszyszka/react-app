{
  "name": "react-app",
  "version": "1.0.0",
  "description": "Pilot Academy Workshop: React.js",
  "main": "src/app.js",
  "scripts": {
    "test":  "echo \"Error no test specified\" && exit 1",
    "start": "http-server dist --silent",
    "watch": "npm start & watchify . -o dist/app.js --verbose --debug --extension .jsx"
  },
  "author": "Przemysław Szyszka",
  "license": "ISC",
  "dependencies": {
    "browserify": "^10.1.3"
  },
  "devDependencies": {
    "http-server": "^0.8.0",
    "watchify": "^3.2.1"
  }
}
